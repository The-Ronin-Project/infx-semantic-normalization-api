# TODO: business logic for the mapping end point
#   In all likelihood, this will not be a file, but a module(s) that replicates the interops code for doing the mapping


def do_mapping():
    # Parse the incoming fhir
    # Identify the source code
    # Attempt lookup of target
    # Build response with mapping or take action on failure to map
    raise NotImplementedError
